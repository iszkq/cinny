# 星火 v1.9.7

- 修复自动更新覆盖安装后，Windows 仍保留失效加密设备导致消息无法解密的问题。
- 启动时自动检查当前设备会话、服务器端设备密钥与本地加密身份是否一致；确认设备身份失效后，安全停用旧会话并引导重新登录创建新设备。
- 保留用户设置、PIN、媒体缓存和 Android 同步快照；网络暂时不可用或请求超时时不会误退出登录。
- 将缺失会话密钥的恢复交由 Matrix SDK 统一处理，减少重复恢复流程造成的竞态。
- 改进解密诊断信息，方便定位设备身份、密钥备份和房间会话问题。
