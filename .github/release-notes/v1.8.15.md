# Starfire v1.8.15

## 修复

- 设备表情验证完成后持久保存可信状态，不再出现弹窗显示完成但设备仍未验证。
- 同步当前设备与目标设备的跨签名，并对暂时失败自动重试，避免验证状态再次丢失。
- 复用同一设备正在进行的验证请求，避免并行请求导致一端完成、另一端取消。
- 验证取消不再触发二次取消，并明确显示接管、超时或表情不一致等原因。

## 验证

- 33 项回归测试、TypeScript、ESLint、生产构建和 225 项资源检查均通过。
