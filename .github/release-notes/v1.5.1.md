## 更新说明

1. 新增云端表情面板，可在表情包面板中按 CF R2 分类浏览云端表情包。
2. 保留原有搜索框，并支持通过最新的 `index.json` 同步新增分类和新增表情。
3. 云端表情索引会在每次登录或刷新页面后获取一次最新数据，不进行持续后台轮询。
4. 点击云端表情会复用现有远程图片/贴纸发送逻辑，网页端和桌面客户端功能保持一致。
5. 桌面客户端随本次版本同步更新云端表情功能，并保持与网页端一致的发送体验。
